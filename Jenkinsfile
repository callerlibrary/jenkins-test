pipeline {
    agent any
    stages {
        stage('build') {
          steps {
                sh 'export PATH=$PATH:/usr/local/bin'
                sh 'npm --version'
            }
        }
    }
}